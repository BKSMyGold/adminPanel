import Axios from "axios";
import { BASE_URL } from "../Constants";

export const sendOtpForOrderCancellation = () => Promise.resolve(true);

export const verifyOtpForOrderCancellation = (otp) => Promise.resolve(true);
